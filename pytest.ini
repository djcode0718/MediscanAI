[pytest]
testpaths = tests
markers =
    integration: Real AI/ML integration tests with heavy model loads (select with '-m integration')
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
